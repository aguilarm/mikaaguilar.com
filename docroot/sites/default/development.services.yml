parameters:
  twig.config:
    debug: true
    auto_reload: true
    cache: true
  http.response.debug_cacheability_headers: false
services:
  cache.backend.null:
    class: Drupal\Core\Cache\NullBackendFactory
